# Netlify SPA Redirects for GVET

# Redirect all non-file requests to index.html for SPA routing
/*    /index.html   200

# API Redirects (if needed)
/api/*  https://vlfjmffbwrmblvlsbsnz.supabase.co/:splat  200

# Force HTTPS (optional - Netlify does this by default)
http://gvet.lt/*  https://gvet.lt/:splat  301!

# Legacy URL redirects (if you have any old URLs to redirect)
# /old-path  /new-path  301
